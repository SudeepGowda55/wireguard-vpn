# Run an EC2 Instance

# Associate an Elastic IP to this server befor running this commands

sudo apt update

# Multiple instructions will be executed carefully set it

curl -L https://install.pivpn.io | bash

# This cmd will finally reboot the system 

# After system reboot

pivpn add

# Copy the conf file it is needed for client. (one conf file should be used only for one client)

# Create Multiple if needed
